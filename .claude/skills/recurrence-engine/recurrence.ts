// [M3] STUB — rule → occurrences per the algorithm in SKILL.md (lazy-on-read,
// idempotent, Asia/Bangkok, byWeekday + exceptions). Implement during M3; mirror
// the canonical version into lib/recurrence/recurrence.ts. No real logic yet.
