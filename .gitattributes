# Normalize line endings to LF in the repo; checkout as-is on all platforms
* text=auto eol=lf

# Binary files — no conversion
*.png binary
*.jpg binary
*.ico binary
*.wasm binary
*.traineddata binary
pnpm-lock.yaml -text
